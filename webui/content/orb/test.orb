
blog blog blog blog

here's some bold{ bold text } and italic{ italic text }, i hope it renders right

code:
height300 { code/highlight {
    function fibonacci(n) {
       if (n <= 1) return n;
       return fibonacci(n - 1) + fibonacci(n - 2);
    }
} }

demo:
demo { gpu/proj_shift {} }

For mathematical expressions, I can write things like math{ \int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2} } inline, or display them in blocks:

math {
E = mc^2

\nabla \times \mathbf{E} = -\frac{\partial \mathbf{B}}{\partial t}
}

Sometimes I want to embed interactive demos. If your browser supports WebGPU, you can play with this fractal:

sandbox {
   fractal {
       type: mandelbrot
       zoom: 1.5
       center: [-0.5, 0]
   }
}

This text tests \{ escape sequences \}!

