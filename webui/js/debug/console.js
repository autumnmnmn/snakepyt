
$css(`

`);

const defaults = {
};

const originals = {
    log: console.log,
    warn: console.warn,
    error: console.error,
    info: console.info,
    debug: console.debug,
};

export async function main(target, spec) {
    spec = { ...defaults, ...spec };


    // todo
}


